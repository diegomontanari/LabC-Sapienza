```txt
Implement the function:

float scl_media(TipoSCL scl);

which returns the average value of the elements in the list. The exercise must be completed without using the previously implemented functions.

Example:

Input: scl: 5->4->3

Output: 4
```