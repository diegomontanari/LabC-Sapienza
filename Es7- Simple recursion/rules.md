```txt
All exercises must be solved using recursive functions. Write tests in the main function to verify that the implemented functions are correct. The use of auxiliary functions is allowed, provided they are recursive. The use of global or static variables is not permitted.
```
